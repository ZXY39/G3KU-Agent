# file_vault_cleanup

Run file vault cleanup. Use dry_run=true first to preview deletions.

## Parameters
- `dry_run`: Preview only without deleting files.
- `target_pct`: Optional target usage percent after cleanup.

## Usage
Use `file_vault_cleanup` only when it is the most direct way to complete the task.
