# task_monitor_list

List all, in-progress, failed, or unread tasks with short descriptions.

## Parameters
- `scope`: 1=all, 2=in_progress, 3=failed, 4=unread_progress

## Usage
Use `task_monitor_list` only when it is the most direct way to complete the task.
