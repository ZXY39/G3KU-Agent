# memory_note

Load detailed note content for a `ref:note_xxxx` entry already visible in `MEMORY.md`.

## Must Use

- The current memory snapshot references `ref:note_xxxx` and you need the detailed body.

## Do Not Use

- Guessing note refs that are not visible in the current memory snapshot.
- Replacing `memory_write` or `memory_delete`.
