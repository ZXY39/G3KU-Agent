# orggraph_control_project

Pause, resume, cancel, or archive an existing org-graph project.

## Parameters
- `project_id`: The project id to control.
- `action`: Control action to perform.

## Usage
Use `orggraph_control_project` only when it is the most direct way to complete the task.
