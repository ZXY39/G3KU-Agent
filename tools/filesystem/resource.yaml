schema_version: 1
kind: tool
name: filesystem
display_name: Filesystem
description: Read, list, write, edit, delete workspace files, and create reviewable patch artifacts.
source:
  type: ''
  url: ''
  ref: ''
current_version:
  summary: ?????????????????
  compare_rule: ???????????????????????????????toolskill ????????????????
  source_of_truth: ????????????resource.yaml ? toolskills ?????
protocol: mcp
mcp:
  transport: embedded
requires:
  tools: []
  bins: []
  env: []
permissions:
  network: false
  filesystem:
    - workspace
settings:
  restrict_to_workspace: false
parameters:
  type: object
  properties:
    action:
      type: string
      enum:
        - read
        - list
        - write
        - edit
        - delete
        - propose_patch
      description: Filesystem action to execute.
    path:
      type: string
      description: Target file or directory path. Relative paths resolve from workspace.
    content:
      type: string
      description: Required when action=write.
    old_text:
      type: string
      description: Required when action=edit or action=propose_patch. Must match exactly once.
    new_text:
      type: string
      description: Replacement text for action=edit or action=propose_patch.
    summary:
      type: string
      description: Optional patch summary when action=propose_patch.
  required:
    - action
    - path
governance:
  family: filesystem
  display_name: Filesystem
  description: Read, list, write, edit, delete workspace files, and create reviewable patch artifacts.
  actions:
    - id: read
      label: Read File
      risk_level: low
      destructive: false
      allowed_roles:
        - ceo
        - execution
        - inspection
    - id: list
      label: List Directory
      risk_level: low
      destructive: false
      allowed_roles:
        - ceo
        - execution
        - inspection
    - id: write
      label: Write File
      risk_level: high
      destructive: true
      allowed_roles:
        - ceo
        - execution
    - id: edit
      label: Edit File
      risk_level: high
      destructive: true
      allowed_roles:
        - ceo
        - execution
    - id: delete
      label: Delete File
      risk_level: high
      destructive: true
      allowed_roles:
        - ceo
    - id: propose_patch
      label: Propose Patch
      risk_level: medium
      destructive: false
      allowed_roles:
        - ceo
        - execution
exposure:
  agent: true
  main_runtime: true
toolskill:
  enabled: true
