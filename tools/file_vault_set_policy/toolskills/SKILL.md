# file_vault_set_policy

Update file vault max storage and cleanup threshold. Only call when user explicitly asks to adjust storage policy.

## Parameters
- `max_storage_gb`: New max storage in GB.
- `threshold_pct`: Cleanup trigger threshold percentage.

## Usage
Use `file_vault_set_policy` only when it is the most direct way to complete the task.
