# orggraph_create_project

Create a new org-graph background project for long-running or multi-step work.

## Parameters
- `prompt`: The project request to execute in the background.
- `preferred_title`: Optional short project title.
- `output_target`: Optional output target label.

## Usage
Use `orggraph_create_project` only when it is the most direct way to complete the task.
