# task_monitor_progress

Inspect one task in detail, including status, tree text, node output, logs, and latest checker output.

## Parameters
- `task_id`: The target project/task id.

## Usage
Use `task_monitor_progress` only when it is the most direct way to complete the task.
