# file_vault_stats

Return file vault capacity, usage, and most-used files.

## Parameters
- `top_n`: How many top-used files to include.

## Usage
Use `file_vault_stats` only when it is the most direct way to complete the task.
