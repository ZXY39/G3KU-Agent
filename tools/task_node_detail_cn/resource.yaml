schema_version: 1
kind: tool
display_name: "鏌ヨ鑺傜偣璇︽儏"
name: task_node_detail
description: View task node detail and related artifacts.
tool_result_delivery_contract: runtime_managed
source:
  type: ''
  url: ''
  ref: ''
current_version:
  summary: Resource-local task runtime tool. Handler and governance live with the resource so discovery can refresh without a host restart.
  compare_rule: Treat manifest, main/, parameter contract, or returned payload changes as version-relevant.
  source_of_truth: resource.yaml, main/, and toolskills together define the effective behavior.
requires:
  tools: []
  bins: []
  env: []
permissions:
  network: false
  filesystem: []
parameters:
  type: object
  properties:
    浠诲姟id:
      type: string
      description: Target task id.
    鑺傜偣id:
      type: string
      description: Target node id.
    detail_level:
      type: string
      enum: [summary, full]
      description: "`summary` returns the light detail view; `full` returns the full execution trace and artifact list."
  required: [浠诲姟id, 鑺傜偣id]
governance:
  family: task_runtime
  display_name: Task Runtime
  description: View task node detail and related artifacts.
  actions:
    - id: node_detail_cn
      label: Task Node Detail
      risk_level: low
      destructive: false
      allowed_roles:
        - ceo
exposure:
  agent: true
  main_runtime: false
toolskill:
  enabled: true

