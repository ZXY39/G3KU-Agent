# load_tool_context

Load the detailed usage guide for a currently visible tool so the CEO can use it correctly.

## Parameters
- `tool_id`: The tool id to load.

## Usage
Use `load_tool_context` only when it is the most direct way to complete the task.
