# web_fetch

Fetch URL and extract readable content (HTML 鈫?markdown/text).

## Parameters
- `url`: URL to fetch
- `extractMode`: 
- `maxChars`: 

## Usage
Use `web_fetch` only when it is the most direct way to complete the task.
