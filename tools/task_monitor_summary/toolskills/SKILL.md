# task_monitor_summary

Summarize unread progress, engineering exceptions, total tasks, active tasks, and failed tasks.

## Parameters
- No parameters.

## Usage
Use `task_monitor_summary` only when it is the most direct way to complete the task.
