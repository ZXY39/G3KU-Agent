{"status": "error", "exit_code": 1, "command": "python -c \"import urllib.request, pathlib; from textwrap import dedent; u='https://raw.githubusercontent.com/openclaw/skills/main/skills/jakelin/ddg-web-search/SKILL.md'; text=urllib.request.urlopen(u,timeout=30).read().decode('utf-8'); outdir=pathlib.Path('skills/ddg-web-search'); (outdir/'references').mkdir(parents=True, exist_ok=True); (outdir/'scripts').mkdir(exist_ok=True); (outdir/'resource.yaml').write_text(dedent('''schema_version: 1\nkind: skill\nname: ddg-web-search\ndescription: 使用 DuckDuckGo Lite 进行免 API Key 的网页搜索，适合作为无搜索 API 时的兜底方案。\ntrigger:\n  keywords:\n    - duckduckgo\n    - ddg\n    - web search\n    - search fallback\n  always: false\nrequires:\n  tools: []\n  bins: []\nentry:\n  main: SKILL.md\n  references: references\n  scripts: scripts\nexposure:\n  agent: true\n  org_graph: true\n'''), encoding='utf-8'); (outdir/'SKILL.md').write_text(dedent('''# DDG Web Search\n\n从上游 `openclaw/skills` 的 `skills/jakelin/ddg-web-search` 迁移而来。\n\n该 skill 提供一个**无需 API Key** 的网页搜索兜底方案：通过 DuckDuckGo Lite 的 HTML 结果页完成关键词检索。适用于默认搜索能力不可用、未配置搜索 API，或需要一个轻量级文本搜索备选路径的场景。\n\n## 何时使用\n\n- 常规网页搜索工具不可用\n- 环境中没有可用的搜索 API Key\n- 只需要文本网页结果，不需要图片/视频/时间过滤\n- 需要一个可手动执行的搜索流程说明\n\n## 输入\n\n提供自然语言搜索词即可，例如：\n\n- `site:example.com release notes 2025`\n- `best sqlite migration tools python`\n- `OpenAI function calling json schema examples`\n\n## 使用方法\n\n### 1. 生成 DuckDuckGo Lite 搜索 URL\n\n将查询词进行 URL 编码后，拼接到：\n\n`https://lite.duckduckgo.com/lite/?q=<encoded query>`\n\n示例：\n\n`https://lite.duckduckgo.com/lite/?q=best%20sqlite%20migration%20tools%20python`\n\n### 2. 抓取结果页\n\n使用当前环境可用的网页抓取/访问能力请求该 URL，并读取返回的 HTML 文本。\n\n### 3. 提取搜索结果\n\n优先提取：\n\n- 结果标题\n- 目标链接\n- 简短摘要\n\n如果需要进一步确认内容，再逐个打开候选结果页。\n\n### 4. 输出建议格式\n\n- 标题\n- 链接\n- 摘要\n- 与当前任务的相关性说明\n\n## 推荐流程\n\n1. 先用精确关键词搜索\n2. 若结果噪声较大，补充限定词：\n   - `site:`\n   - 具体产品名 / 版本号\n   - `github`\n   - `docs`\n   - `official`\n3. 对前 3–5 个结果做二次打开验证\n4. 明确标注哪些信息来自搜索结果页、哪些来自落地页面\n\n## 限制\n\n- 不支持可靠的时间过滤\n- 仅适合文本网页结果，不适合图片/视频搜索\n- 结果来源和排序可能与其他搜索引擎不同\n- 某些站点可能触发跳转、反爬或内容缺失\n- 本仓库当前未内置专门的 `web_fetch` 工具；因此这里保留为**可迁移的流程型 skill**，使用时需结合环境中实际可用的网页访问工具或外部浏览能力\n\n## 与上游的最小适配差异\n\n- 上游 skill 面向带有 `web_fetch` 能力的代理环境，本文档直接提到该能力。\n- 当前 G3KU 工作区中未发现同名内置工具，因此迁移时将其适配为**流程说明型 skill**，不强绑定具体工具实现。\n- 保留了“DuckDuckGo Lite + 无 API Key + 兜底搜索”的核心用途，但把执行步骤改写为更通用的中文说明，便于在不同运行环境中复用。\n\n## 参考资料\n\n详见 `references/upstream.md`。\n'''), encoding='utf-8'); (outdir/'references'/'upstream.md').write_text(dedent('''# Upstream Reference\n\n- Source repository: https://github.com/openclaw/skills\n- Source path: `skills/jakelin/ddg-web-search`\n- Source raw file: https://raw.githubusercontent.com/openclaw/skills/main/skills/jakelin/ddg-web-search/SKILL.md\n\n## Import notes\n\nThis local copy is based on the upstream `SKILL.md` available at migration time.\nThe upstream resource appears to be a **skill**, not a tool: it is a directory under `skills/` and is represented as a single markdown skill document with frontmatter describing usage.\n\n## Compatibility adaptation\n\n- Kept the original purpose: DuckDuckGo Lite based web search without API keys.\n- Adapted wording for G3KU and converted the document into the local skill layout:\n  - `skills/ddg-web-search/resource.yaml`\n  - `skills/ddg-web-search/SKILL.md`\n  - `skills/ddg-web-search/references/upstream.md`\n- Because the current workspace does not expose a dedicated `web_fetch` built-in tool in this task context, the migrated version documents a generic workflow instead of binding to a specific fetch tool.\n\n## Upstream excerpt summary\n\nThe upstream document states that the skill is used for web search without an API key via DuckDuckGo Lite, especially as a fallback when regular web search is unavailable or lacks configuration. It also notes limitations such as lack of reliable date filtering and text-only results.\n'''), encoding='utf-8'); print('created', outdir)\"", "stdout_ref": "", "stderr_ref": "artifact:artifact:956f09e724e5", "head_preview": "STDERR:\nFile \"<string>\", line 1\n    import urllib.request, pathlib; from textwrap import dedent; u='https://raw.githubusercontent.com/openclaw/skills/main/skills/jakelin/ddg-web-search/SKILL.md'; text=urllib.request.urlopen(u,timeout=30).re...", "tail_preview": "STDERR:\nFile \"<string>\", line 1\n    import urllib.request, pathlib; from textwrap import dedent; u='https://raw.githubusercontent.com/openclaw/skills/main/skills/jakelin/ddg-web-search/SKILL.md'; text=urllib.request.urlopen(u,timeout=30).re...", "line_count": 5, "next_actions": ["content.search", "content.open"], "error": "Exit code: 1"}