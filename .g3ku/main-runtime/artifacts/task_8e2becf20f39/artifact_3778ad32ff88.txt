{"ok": true, "skill_id": "add-tool", "uri": "g3ku://skill/add-tool", "level": "l1", "content": "Title: add-tool\nSummary: 新增、迁移、合并、重构或审核 `tools/<tool_id>/` 下的工具资源时使用。适用于内置工具与外置工具双模式注册，重点覆盖 `tool_type`、`install_dir`、`resource.yaml`、`toolskills/SKILL.md`、安装/更新流程、删除安全原则，以及“工具必须异步执行、不得阻塞整个项目”的接入约束。\nKey Points: 添加工具 | 当任务是新增、迁移、合并、重构或审核 `tools/<tool_id>/` 下的工具资源时，使用本规范。 | 本仓库现在区分两类工具： | - `internal`：工具本体位于 `tools/<tool_id>/main/`，可直接进入函数工具列表。 | - `external`：第三方项目安装在 `tools/` 之外，`tools/<tool_id>/` 只负责注册，不包含 `main/`。 | 目标 | - 统一注册入口仍然是 `tools/<tool_id>/`。 | - `resource.yaml` 必须明确工具类型、来源、版本、参数、权限、治理和安装位置。 | - 第三方且需要接收上游更新的工具，默认走 `external`，不要把上游项目继续塞进 `tools/<tool_id>/main/`。 | - `toolskills/SKILL.md` 除了说明如何使用，还必须说明如何安装、如何更新。 | - 所有...", "l0": "add-tool", "l1": "Title: add-tool\nSummary: 新增、迁移、合并、重构或审核 `tools/<tool_id>/` 下的工具资源时使用。适用于内置工具与外置工具双模式注册，重点覆盖 `tool_type`、`install_dir`、`resource.yaml`、`toolskills/SKILL.md`、安装/更新流程、删除安全原则，以及“工具必须异步执行、不得阻塞整个项目”的接入约束。\nKey Points: 添加工具 | 当任务是新增、迁移、合并、重构或审核 `tools/<tool_id>/` 下的工具资源时，使用本规范。 | 本仓库现在区分两类工具： | - `internal`：工具本体位于 `tools/<tool_id>/main/`，可直接进入函数工具列表。 | - `external`：第三方项目安装在 `tools/` 之外，`tools/<tool_id>/` 只负责注册，不包含 `main/`。 | 目标 | - 统一注册入口仍然是 `tools/<tool_id>/`。 | - `resource.yaml` 必须明确工具类型、来源、版本、参数、权限、治理和安装位置。 | - 第三方且需要接收上游更新的工具，默认走 `external`，不要把上游项目继续塞进 `tools/<tool_id>/main/`。 | - `toolskills/SKILL.md` 除了说明如何使用，还必须说明如何安装、如何更新。 | - 所有...", "path": "skills\\add-tool\\SKILL.md"}