已完成接入并落地为本地 skill。结论如下：1) 资源类型评估：该上游资源更适合作为 skill，而不是 tool。原因是上游目标路径 `openclaw/skills/skills/jakelin/ddg-web-search` 提供的是说明性 `SKILL.md` 工作流，核心能力依赖宿主环境已有的网页抓取能力（如 `web_fetch`），未发现需要单独注册的可执行程序、内部函数工具、安装脚本或独立运行时。2) 预检查结果：当前仓库存在 `skills/` 与 `tools/` 目录；本地原先不存在 `skills/ddg-web-search`，因此未覆盖现有资源。3) 上游获取情况：已成功通过 `raw.githubusercontent.com` 获取上游 `SKILL.md` 内容；直接 `git clone https://github.com/openclaw/skills` 在当前 Windows 环境失败，阻塞原因是上游仓库其他路径含 Windows 非法文件名字符（如冒号 `:`），导致 checkout 失败。这不是目标 skill 本身的问题，而是整个仓库在当前平台上的兼容性问题。4) 本地落地路径：- `skills/ddg-web-search/resource.yaml` - `skills/ddg-web-search/SKILL.md` - `skills/ddg-web-search/references/upstream-SKILL.md` - `skills/ddg-web-search/references/notes.md` 5) 已完成的适配：- 按当前 G3KU 本地 skill 结构创建目录与元数据；- 新增 `resource.yaml`，声明其为 `kind: skill`；- 编写适配后的本地 `SKILL.md`，说明用途、适用场景、使用方式、局限性与适配说明；- 保留上游原始文档到 `references/upstream-SKILL.md` 以便后续对照；- 记录接入说明与已知限制到 `references/notes.md`。6) 是否可直接使用：基本可直接使用，但有前提。该 skill 本身不提供搜索引擎 API 或抓取实现，它是一个“提示/流程型 skill”。只有在当前运行环境具备外网访问能力，并且代理可使用 `web_fetch` 或等效网页抓取能力时，才可实际完成 DuckDuckGo Lite 搜索。如果环境没有这些能力，则该 skill 只能作为说明文档存在，不能独立生效。7) 后续注意事项：- 当前 `resource.yaml` 未精确声明 `web_fetch` 这类宿主侧函数能力依赖，因此真实可用性仍取决于运行时；- 若后续希望更强 discoverability，可将其加入相关索引或在联网/搜索类 skill 中互相引用；- 若要持续跟踪上游更新，建议不要在 Windows 上直接整库 clone，可改用 GitHub 单文件/raw 拉取、归档下载，或在 Linux/WSL 环境处理后再同步。8) 阻塞与建议：本次没有阻塞到接入完成，但存在上游仓库整库克隆失败的问题。建议后续若要批量迁移 openclaw/skills 中更多资源，优先使用 WSL/Linux 环境或 API/raw/zip 方式提取目标子目录，避免 Windows 文件名兼容问题。总体状态：接入成功，已落地到 `skills/ddg-web-search`，属于可用的本地 skill 适配版本。