from __future__ import annotations

from pathlib import Path

PROMPTS_DIR = Path(__file__).resolve().parent / 'prompts'

STRICT_CHILD_SPAWN_POLICY_PROMPT = """默认在当前节点完成任务。派生子节点是严格例外，不是默认策略。

先按第一性原理收敛：先找最直接的对象、事实、来源、主路径或关键矛盾，再验证。不要因为任务复杂、范围大、资料多、候选多、目录多、章节多、来源多、搜索命中多，或感觉并行更快，就自动拆分。

只有在你已读取核心材料或直接证据后，并且确认满足以下至少一条时，才允许派生：
1. 问题极度开放，必须在执行中灵活探索旁支；
2. 存在两个以上冲突领域，必须隔离推理上下文；
3. 需要沿多条独立路径并行推进，且并行能显著提升效率。

若不满足以上任一条，禁止派生。

派生前必须明确：
1. 已确认的主路径/主事实；
2. 当前缺口；
3. 满足了哪条铁律以及原因；
4. 为什么当前节点不能独立完成；
5. 每个子节点的精确范围与交付物。

说不清以上五点，禁止派生。

子节点只能按问题缺口拆，不能按目录、章节、来源表面分类、关键词命中或模糊候选拆。若当前节点只做了初筛、枚举、搜索、范围收敛或候选整理，而未完成关键验证，也禁止继续派生。

一旦找到主路径，后续动作必须服务于主路径；与主路径无关的扩散搜索视为偏航，必须停止。

如果不确定是否应该派生，默认答案必须是：不派生。"""


def load_prompt(name: str) -> str:
    return (PROMPTS_DIR / name).read_text(encoding='utf-8')
