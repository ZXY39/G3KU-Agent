schema_version: 1
kind: skill
name: "ddg-web-search"
description: "Web search without an API key using DuckDuckGo Lite via web_fetch. Only use in runtimes where the visible tool list includes web_fetch. Provides titles, URLs, and snippets for research queries."
trigger:
  keywords:
    - "ddg-web-search"
    - "ddg-search"
  always: false
requires:
  tools:
    - web_fetch
  bins: []
  env: []
content:
  main: SKILL.md
exposure:
  agent: true
  main_runtime: true
