# Browser Operation

Use the browser tool to inspect pages, open URLs, search in visible or headless browser context, and summarize what you observed.

Rules:
- Prefer short, evidence-based summaries.
- When browser actions fail, report the concrete blocking reason and the next minimal recovery step.
