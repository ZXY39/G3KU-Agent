schema_version: 1
name: cron
display_name: Cron
description: Scheduled reminder and cron management skill.
version: "1.0.0"
content:
  main: SKILL.md
governance:
  enabled_by_default: true
  allowed_roles:
    - ceo
    - execution
    - execution
  editable_files:
    - SKILL.md
    - skill.yaml
  risk_level: medium
runtime:
  load_action: load
metadata:
  tags:
    - builtin

