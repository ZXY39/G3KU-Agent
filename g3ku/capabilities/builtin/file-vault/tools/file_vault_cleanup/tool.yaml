schema_version: 1
name: file_vault_cleanup
label: file_vault_cleanup
description: Run file vault cleanup
entrypoint: g3ku.capabilities.builtin_factories:build_file_vault_cleanup
permissions:
  network: false
  filesystem:
    - workspace
streaming: false
admin_only: false

